{
  "version": 1,
  "adapters": [
    {
      "id": "orchAgent.hooks.builtin",
      "type": "builtin",
      "enabled": true
    }
  ],
  "hooks": [
    {
      "id": "orchAgent.session.audit",
      "adapter": "orchAgent.hooks.builtin",
      "description": "Dry-run audit hook for session lifecycle events",
      "enabled": true,
      "events": ["session.start", "session.end"]
    },
    {
      "id": "orchAgent.tool.audit",
      "adapter": "orchAgent.hooks.builtin",
      "description": "Dry-run audit hook for tool lifecycle events",
      "enabled": true,
      "events": ["tool.before", "tool.after"]
    }
  ]
}
